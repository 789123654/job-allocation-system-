# Secrets — never committed (DEPLOYMENT.md secrets-handling requirement).
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Rust (src-tauri/, once scaffolded)
target/

# backend/loadtest/ generated artifacts — a fresh throwaway RSA keypair + signed tokens + seeded
# identities every run, regenerated by generate_keys.py/seed.py, never meant to be committed.
backend/loadtest/private_key.pem
backend/loadtest/www/
backend/loadtest/identities.json

# OS
.DS_Store
Thumbs.db
